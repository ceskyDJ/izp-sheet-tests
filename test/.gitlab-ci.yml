image: gcc

build:
  stage: build
  script:
    - gcc -std=c99 -Wall -Wextra -Werror sheet.c -o tmp/sheet
  artifacts:
    paths:
      - tmp/sheet

test:
  stage: test
  image:
    name: php:7.4
  script:
    - php -v
    - php test/test.php